[build]
builder = "NIXPACKS"
buildCommand = "cp prisma/schema.postgres.prisma prisma/schema.prisma && npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
